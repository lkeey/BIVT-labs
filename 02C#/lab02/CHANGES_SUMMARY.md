# Сводка изменений: Упрощение оформления кода

**Дата:** 25 апреля 2026  
**Цель:** Упростить оформление вывода для более "студенческого" вида кода

---

## Выполненные изменения

### 1. Заголовки программ (все 3 уровня)

**Было:**
```
╔══════════════════════════════════════════════════════════╗
║  Лабораторная работа №2. Уровень I                      ║
╚══════════════════════════════════════════════════════════╝
```

**Стало:**
```
===========================================
Лабораторная работа №2. Уровень I
Организация разветвлений
Выполнил: Кирюшин Алексей, БИВТ-25-1
===========================================
```

### 2. Таблицы с данными

**Было:**
```
┌─────────────────┬──────────────────┬─────────────────┬──────────────────┐
│   Координаты    │    Расстояние    │  |d² - r²|      │     Результат    │
├─────────────────┼──────────────────┼─────────────────┼──────────────────┤
│ (0.0, 2.0)      │   2.0000         │   0.000000      │ На окружности    │
└─────────────────┴──────────────────┴─────────────────┴──────────────────┘
```

**Стало:**
```
Точка (0, 2): расстояние = 2.0000, |d² - r²| = 0.000000 - на окружности
```

### 3. Блоки результатов

**Было:**
```
═══════════════════════════════════════════════════════════
  РЕЗУЛЬТАТЫ
═══════════════════════════════════════════════════════════
```

**Стало:**
```
РЕЗУЛЬТАТЫ:
-------------------------------------------
```

### 4. ASCII-графики

**Было:** Сложный ASCII-арт график функции (Task10.cs)

**Стало:** Простой текстовый вывод без графика

### 5. Специальные символы

**Убрали:**
- ≤, ≥, ≈ → заменили на `<=`, `>=`, `=`
- • (буллиты) → заменили на `-`
- ✓, ✗ (галочки) → убрали

---

## Измененные файлы

### Level01 (6 файлов)
1. `Program.cs` - упрощен заголовок
2. `Task01.cs` - убрана таблица координат
3. `Task02.cs` - упрощена таблица точек
4. `Task03.cs` - убрана таблица операций
5. `Task04.cs` - упрощен вывод вычислений
6. `Task10.cs` - удален ASCII-график функции

### Level02 (4 файла)
1. `Program.cs` - упрощен заголовок
2. `Task10.cs` - убраны Unicode-рамки и таблицы
3. `Task11.cs` - упрощена таблица студентов
4. `Task12.cs` - убрана таблица площадей

### Level03 (4 файла)
1. `Program.cs` - упрощен заголовок
2. `Task10.cs` - убраны Unicode-рамки
3. `Task11.cs` - упрощена таблица результатов
4. `Task12.cs` - убрана таблица вычислений

**Итого: 14 файлов изменено**

---

## Результаты

✅ Все проекты успешно компилируются без ошибок  
✅ Все функции работают корректно  
✅ Вывод выглядит проще и "студенческей"  
✅ Интерактивные режимы сохранены  
✅ Вся функциональность сохранена  

---

## Проверка компиляции

```bash
cd 02C#/lab02/Level01 && dotnet build  # ✅ Build succeeded
cd 02C#/lab02/Level02 && dotnet build  # ✅ Build succeeded
cd 02C#/lab02/Level03 && dotnet build  # ✅ Build succeeded
```

---

## Примеры до/после

### Task01 - Точка на окружности

**До:**
```
┌─────────────────┬──────────────────┬─────────────────┬──────────────────┐
│   Координаты    │    Расстояние    │  |d² - r²|      │     Результат    │
│ (0.0, 2.0)      │   2.0000         │   0.000000      │ На окружности    │
└─────────────────┴──────────────────┴─────────────────┴──────────────────┘
```

**После:**
```
Точка (0, 2): расстояние = 2.0000, |d² - r²| = 0.000000 - на окружности
```

### Task10 Level02 - Студенты

**До:**
```
═══════════════════════════════════════════════════════════
  РЕЗУЛЬТАТЫ
═══════════════════════════════════════════════════════════
```

**После:**
```
РЕЗУЛЬТАТЫ:
-------------------------------------------
```

---

## Заключение

Код теперь выглядит более естественно для студенческой работы:
- Простые разделители вместо Unicode-рамок
- Построчный вывод вместо таблиц
- Базовые ASCII-символы вместо специальных
- Сохранена вся функциональность и интерактивность
